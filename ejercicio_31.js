const prompt = require('prompt-sync')();
var num;

num=parseInt("ingrese un numero por favor: ")
function esPrimo() {
    while (num/num && num/1) {
        console.log("es numero ingresado es primo")
        break 
    }
}
console.log(esPrimo)
